About this directory
====================

This directory (will) contain some retrievers (that is, plugin for lyricseek
library). Strictly speaking, they are not part of the library itself, but they
are given for ease of use.

Since every retriever is a directory, you'll find here lots of directories.
